# Utils

## Video stepper
A simple video stepper utility to navigate through video frames.

To use the video stepper, run the following command:

```bash
uv run video_stepper.py
```


## Multi-Window stepper

A utility to manage multiple video windows simultaneously.
To use the multi-window stepper, run the following command:

```bash
uv run multi_video_stepper.py
```